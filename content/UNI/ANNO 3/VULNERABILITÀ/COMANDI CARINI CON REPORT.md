-k o --insecure per https con certificati

gobuster vhost per trovare  il vhost con subdomains dns

leggere chiave ssh `homeutente/.ssh/id_rsa`
chmod 600 per il file per eseguirlo
path hijacking quando un comando viene eseguito senza path assoluto
$PATH viene letto in ordine di scrittura
vedi foto

usare un server python per far scaricare i file che vuoi a quella macchina che stai attaccando
`python -m http.server 9999`
e poi fai curl dalla macchina target
ls -la etc/cron.* può essere utile per far vedere i cronjob di root?
